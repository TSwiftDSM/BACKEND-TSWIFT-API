<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>

<body>
    <form action="/qualitativeStage" method="post">

        <% for( let produtos of qualidadeProdutos) { %>
            <div>
                <%= produtos.Produto.nomeProduto %>
            </div>

            <div>
                <%= produtos.TesteQualidade.nomeTeste %>
            </div>
            <input type="radio" value="true" name="aprovado<%= produtos.Produto.id %><%= produtos.TesteQualidade.id %>" id="sim" > 
                <label for="sim">Sim</label>
            <input type="radio" value="false" name="aprovado<%= produtos.Produto.id %><%= produtos.TesteQualidade.id %>" id="nao">
                <label for="nao">Não</label>
           
            
            <% } %>
            <input type="hidden" name="qualidadeProdutos" value="<%= JSON.stringify(qualidadeProdutos) %>">
            <input type="submit" value="Testar">
    </form>
</body>

</html>
