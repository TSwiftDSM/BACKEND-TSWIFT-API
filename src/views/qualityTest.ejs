<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>

<body>
    <form action="" method="post">
        <% Object.entries(qualitiesProducts).forEach(([key, value])=> { %>

            <% Object.entries(value).forEach(([key, value])=> { %>
               
                <div>
                    <%= value["name"] %>
                </div>

                <div>
                    <%= value["productName"] %>
                </div>
                <% }); %>
                <input type="checkbox" name="<%= key %>_chkTeste" id="">



                   
                    <!-- <h1><%= key %> <%= value["Product"] %></h1>
            <h3><%= key %> <%= value["Product"] %></h3>
            <input type="text" name="<%= key %>_weight" required>
            <h3>Quantidade</h3>
            <input type="text" name="<%= key %>_qtt" required> -->
                    <% }); %>
    </form>
</body>

</html>